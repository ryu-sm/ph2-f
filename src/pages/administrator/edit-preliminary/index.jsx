import { AdMainWrapper } from '@/containers';
import { useIsManager } from '@/hooks';

export const EditPreliminary = () => {
  const isManager = useIsManager();
  return <AdMainWrapper>111</AdMainWrapper>;
};
